import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    container: {
        margin: 20,
    },
    title:{
        color:'black',
        fontSize:30,
        fontWeight:'bold'
    },
    
})